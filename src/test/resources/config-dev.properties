# Development configuration - single browser for speed
# 50ms is the default slow mo is recommended for handle flakiness
browsers=chromium
headless=true
slow.mo=50
default.timeout=30000
retry.count=2
reports.dir=test-results/reports
port=8080